import sys

from NERO.constants import *

# number of steps per lifetime
# in battle, the agents can only die by being shot and so their lifetime is
# maxint
lifetime = sys.maxint

TEAMS = ["blue", "red"]
TEAM0 = TEAMS[0]
TEAM1 = TEAMS[1]
